describe("Data Type", () => {
    it("should say hello", () => {
        let name: string = "Fadjrir";
        let balance: number = 10000;
        let isVip: boolean = true;

        console.info(name)
        console.info(balance)
        console.info(isVip)
    });
})